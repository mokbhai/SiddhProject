// console.log('Prints in console');

// Function & str parameter
/// Note: use `` to print variables
const greet = (str) => 
{
    console.log(`Print variables, ${str}`);
}

// calling function
greet(`ss`);
